# complexity_positive

Seeded issue: `tangled` has nested `if let` + `match` with guarded arms,
pushing cognitive complexity well above 10.

Expected verdict: Cognitive Complexity section FAILs with a finding on
`tangled`. Overall WorstSeverity is FAIL.
