# sizes_positive

Seeded issue: `long_func` is ~60 lines of straight-line statements,
exceeding the default 50-line function threshold without tripping the
complexity threshold.

Expected verdict: Code Sizes FAIL with a finding on `long_func`.
