# complexity_positive

Seeded issue: `tangled` mixes nested ternaries, `try/catch`, a `switch`
with multiple cases, and long `&&`/`||` chains — cognitive complexity
well above 10.

Expected verdict: Cognitive Complexity FAIL with a finding on `tangled`.
